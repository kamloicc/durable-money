apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: account-staging
  namespace: argocd

  annotations:
    argo.octopus.com/project: northfield-account-service
    argo.octopus.com/environment: staging

spec:
  project: default

  source:
    repoURL: https://github.com/kamloicc/durable-money.git
    targetRevision: main
    path: gitops/account-service/overlays/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: durable-money-staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=false
