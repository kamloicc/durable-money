apiVersion: v1
kind: Service
metadata:
  name: account-service
  labels:
    app.kubernetes.io/name: account-service
    app.kubernetes.io/part-of: durable-money
spec:
  selector:
    app.kubernetes.io/name: account-service

  ports:
    - name: http
      port: 9080
      targetPort: http
